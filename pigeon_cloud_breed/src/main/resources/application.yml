server:
  port: 8001

spring:
  application:
    name: pigeon-breed-service

  profiles:
    active: dev

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/dove_cloud_farming?useUnicode=true&characterEncoding=utf8&serverTimezone=GMT%2B8&useSSL=false
    username: root
    password: a2690357721
    hikari:
      max-lifetime: 30000
      minimum-idle: 3
      maximum-pool-size: 10
  jackson:
      date-format: yyyy-MM-dd HH:mm:ss
      time-zone: GMT+8

  redis:
    host: 120.77.156.205
    port: 6666
    password: 711711711.com
    database: 15
    timeout: 1800000
    lettuce:
      pool:
        max-active: 20
        max-wait: 5
        max-idle: 5
        min-idle: 0

mybatis-plus:
  mapper-locations: classpath:com/dove/processing/mapper/xml/*.xml  #默认就是任意mapper下的xml文件下的*xml
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      logic-delete-field: flag
      logic-delete-value: 1 # 逻辑已删除值(默认为 1)
      logic-not-delete-value: 0 # 逻辑未删除值(默认为 0)